# Documentación de planificación (roadmaps, specs de diseño, notas de
# análisis) generada durante el desarrollo — referencia local, no forma
# parte del código del producto ni se publica en el repo.
docs/
