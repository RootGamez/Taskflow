# Suite HTTP (APITestCase) reservada para WP-T. El stub
# `apply_template_items` (apps/tickettemplates/services.py) y el contrato
# de `template_id` en `TicketCreateSerializer` ya estan probados en
# apps/tickets/tests.py (docs/PHASE_4_PLAN.md seccion 3.6).
