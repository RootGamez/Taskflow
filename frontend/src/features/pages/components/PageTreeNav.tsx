interface PageTreeNavProps {
  workspaceSlug: string;
}

/**
 * Stub de WP-0A (docs/PHASE_4_PLAN.md seccion 3, D3): componente real con
 * la firma final, devuelve `null`. Se monta debajo de la entrada
 * "Paginas" en `Sidebar.tsx` (D18: colapsado por defecto, arbol con
 * chevrons por nodo con hijos). WP-P (Ola 1) reemplaza unicamente el
 * cuerpo de este archivo sin volver a tocar el sidebar.
 */
export function PageTreeNav(_props: PageTreeNavProps) {
  return null;
}
